exports.up = function(knex, Promise) {
    return Promise.all([
	knex.schema.table('bookings', function(table) {
	    table.dropColumn('improvments');
	}),
	knex.schema.table('bookings', function(table) {
	    table.specificType('improvments', 'integer[]');
	})
    ]);
};

exports.down = function(knex, Promise) {
    return Promise.all([
	knex.schema.table('bookings', function(table) {
	    table.dropColumn('improvments');
	    table.specificType('improvments', 'integer');
	})
    ]);
};
