exports.up = function(knex, Promise) {
    return Promise.all([
	knex.schema.alterTable('creditcards', function(table) {
            table.integer('coach_id');
	}),
	knex.raw('ALTER TABLE creditcards ADD CONSTRAINT coach_id_foreign_key FOREIGN KEY (coach_id) REFERENCES coach (id) MATCH FULL;'),
    ]);
};

exports.down = function(knex, Promise) {
    return knex.schema.alterTable('creditcards', function(table) {
        table.dropColumn('coach_id');
    });
};
