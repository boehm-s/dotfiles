exports.up = function(knex, Promise) {
    return Promise.all([
	knex.schema.createTable('bookings_charge', function (table) {
            table.increments().primary();
	    table.foreign('bookingId').references('bookings.id');
	    table.foreign('userId').references('users.id');
	    table.string('chargeId');
	})]);
};

exports.down = function(knex, Promise) {
    return Promise.all([
	knex.schema.dropTable('bookings_charge')
    ]);
};
