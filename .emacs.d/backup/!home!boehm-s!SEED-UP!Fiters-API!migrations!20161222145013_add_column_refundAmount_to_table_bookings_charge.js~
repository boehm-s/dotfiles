exports.up = function(knex, Promise) {
    return Promise.resolve(0).then(function() {
        return Promise.all([
            knex.schema.alterTable('coaches', function(table) {
                table.string('sponsor_code');
                table.string('sponsor').references('id');
            })
        ])
    });
};

exports.down = function(knex, Promise) {
    return knex.schema.alterTable('coaches', function(table) {
        table.dropColumn('sponsor_code');
        table.dropColumn('sponsor');
    });
};
